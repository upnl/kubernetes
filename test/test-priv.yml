apiVersion: v1
kind: Pod
metadata:
  name: test-ecr
spec:
  containers:
  - name: ubuntu
    image: ubuntu
    command:
      - tail
      - -f
      - /dev/null
    securityContext:
      privileged: true
